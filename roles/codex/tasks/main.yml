---
# ── Prerequisites check ──

- name: Check if nvm is available
  stat:
    path: "{{ user_home }}/.nvm/nvm.sh"
  register: nvm_available

- name: Fail if nvm is not installed (run nodejs role first)
  fail:
    msg: "nvm is not installed. Run the nodejs role first: ansible-playbook site.yml --tags nodejs"
  when: not nvm_available.stat.exists

- name: Check if uv is available (needed for fetch MCP)
  command: which uv
  register: uv_available
  changed_when: false
  failed_when: false

# ── Install Codex CLI ──

- name: Check if codex is installed
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    npm ls -g @openai/codex 2>/dev/null
  args:
    executable: /bin/bash
  register: codex_check
  changed_when: false
  failed_when: false

- name: Install OpenAI Codex CLI via npm (nvm)
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    npm i -g @openai/codex
  args:
    executable: /bin/bash
  when: codex_check.rc != 0

# ── MCP servers ──
# Codex stores MCP in ~/.codex/config.toml under [mcp_servers.<name>]

- name: Read codex config.toml
  slurp:
    src: "{{ user_home }}/.codex/config.toml"
  register: codex_config_raw
  failed_when: false

- name: Set codex config content
  set_fact:
    codex_config: "{{ (codex_config_raw.content | default('') | b64decode) if codex_config_raw.content is defined else '' }}"

- name: Configure Codex MCP — context7 (library documentation)
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    codex mcp add context7 -- npx -y @upstash/context7-mcp
  args:
    executable: /bin/bash
  when: "'[mcp_servers.context7]' not in codex_config"

- name: Configure Codex MCP — memory (persistent knowledge graph)
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    codex mcp add memory --env MEMORY_FILE_PATH={{ user_home }}/.local/share/codex/memory.jsonl -- npx -y @modelcontextprotocol/server-memory
  args:
    executable: /bin/bash
  when: "'[mcp_servers.memory]' not in codex_config"

- name: Ensure Codex memory directory exists
  file:
    path: "{{ user_home }}/.local/share/codex"
    state: directory

- name: Configure Codex MCP — fetch (web documentation retrieval)
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    codex mcp add fetch -- uvx mcp-server-fetch
  args:
    executable: /bin/bash
  when:
    - "'[mcp_servers.fetch]' not in codex_config"
    - uv_available.rc == 0

- name: Configure Codex MCP — sequential-thinking (structured reasoning)
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    codex mcp add sequential-thinking -- npx -y @modelcontextprotocol/server-sequential-thinking
  args:
    executable: /bin/bash
  when: "'[mcp_servers.sequential-thinking]' not in codex_config"

- name: Configure Codex MCP — github (code search, issues, PRs)
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    codex mcp add github --env GITHUB_PERSONAL_ACCESS_TOKEN={{ github_token }} -- npx -y @modelcontextprotocol/server-github
  args:
    executable: /bin/bash
  when:
    - "'[mcp_servers.github]' not in codex_config"
    - github_token is defined
    - github_token | length > 0

# ── Remove obsolete MCP servers ──

- name: Remove playwright MCP if present
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    codex mcp remove playwright
  args:
    executable: /bin/bash
  when: "'[mcp_servers.playwright]' in codex_config"

- name: Remove sentry MCP if present
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    codex mcp remove sentry
  args:
    executable: /bin/bash
  when: "'[mcp_servers.sentry]' in codex_config"

# ── Skills: curated (from openai/skills) ──

- name: Install curated codex skills
  shell: |
    export NVM_DIR="{{ user_home }}/.nvm"
    . "$NVM_DIR/nvm.sh"
    python3 {{ user_home }}/.codex/skills/.system/skill-installer/scripts/install-skill-from-github.py \
      --repo openai/skills --path skills/.curated/{{ item }}
  args:
    executable: /bin/bash
    creates: "{{ user_home }}/.codex/skills/{{ item }}"
  loop:
    - pdf
    - doc

# ── Skills: superpowers (clone + symlink) ──

- name: Clone superpowers skills repo
  git:
    repo: https://github.com/obra/superpowers.git
    dest: "{{ user_home }}/.codex/superpowers"
    update: true

- name: Ensure agents skills directory exists
  file:
    path: "{{ user_home }}/.agents/skills"
    state: directory

- name: Symlink superpowers skills
  file:
    src: "{{ user_home }}/.codex/superpowers/skills"
    dest: "{{ user_home }}/.agents/skills/superpowers"
    state: link
    force: true

# ── Skills: claude-skills / fullstack-dev-skills (clone + symlink) ──

- name: Clone claude-skills repo
  git:
    repo: https://github.com/Jeffallan/claude-skills.git
    dest: "{{ user_home }}/.codex/claude-skills"
    update: true

- name: Symlink claude-skills skills
  file:
    src: "{{ user_home }}/.codex/claude-skills/skills"
    dest: "{{ user_home }}/.agents/skills/claude-skills"
    state: link
    force: true
